<nav class="navbar navbar-fixed-top" role="navigation">
	<div class="container-fluid">
	    <div class="navbar-header">
	         <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
	            <span class="sr-only">Toggle navigation</span>
	            <span class="icon-bar"></span>
	            <span class="icon-bar"></span>
	        	<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="/">Peoplr</a>
	    </div>

	    <div class="navbar-footer navbar-right">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar">
	            <span class="sr-only">Toggle navigation</span>
			</button>
			<a class="navbar-brand pull-right" href="/about">about</a></li>
			<a class="navbar-brand pull-right" href="/spread">spread a word</a></li>
		</div>

	    <div class="row">
	      	<div class="col-md-6">
	       		<div style="padding:10px;">
					<%= form_tag("/search", :method => 'get', :enforce_utf8 => false, :class => "navbar-search form-search") do %>
    				<%= text_field_tag(:q, params[:q], :class => "form-control search-query-nav") %>
					<% end %>
				</div>	
        	</div>
        </div>


		<div class="instant-tab">
		    <ul class="tab-menu">
		    	<li class="button-menu-item">
		      		<a href="">photos</a></li>
		    	<li class="button-menu-item">
		      		<a href="">about</a></li>
		    </ul>

		    <div id="instantCarousel" class="instant-tab-content-wrap carousel slide">
			    <div class="instant-tab-content carousel-inner">
					<% if @statuses.size != 0 %>
						<% @statuses.each do |status| %>
							<% if status[:entities].size == 4 %>
							<% else %>
								<%= image_tag(status[:entities][:media][0][:media_url], :height => "160px")  %>
							<% end %>
						<% end %>
					<% else %>
						<p>No image was found for "<%= @query %>"! </p>
					<% end %>
			    </div>
			    <a class="left carousel-control" href="#instantCarousel" data-slide="prev">‹</a>
                <a class="right carousel-control" href="#instantCarousel" data-slide="next">›</a>

			</div>

		</div>

    </div>
</nav>


<div class="search-wrap center-block">
	<% if @result.size != 0 %>
		<% @result.each do |user| %>
			<div class="search-content">
				<div class="item-image">
					<%= link_to "https://twitter.com/#{user.screen_name}", :target => "_blank" do %>
						<%= image_tag(user.profile_image_url.to_s.sub("_normal", ""), :size => "160x160", :class => "img-rounded")  %>
					<% end %>
				</div>

				<div class="item-description">
					<div class="item-header">
						<%= link_to "https://twitter.com/#{user.screen_name}", :target => "_blank" do %>
							<strong><%= "#{user.name}" %></strong>
						<% end %>
						<em><%= "   @#{user.screen_name}" %></em>
					</div>
					<div class="item-footer">
						<p><%= "Followers: #{user.followers_count}" %></p>
						<p><%= Date.parse(user.created_at.to_s) %></p>
						<% if user.location.present? %>
							<p><%= "GEO: #{user.location}" %></p>
						<% else %>
							<p><%= "GEO: N/A" %></p>
						<% end %>
						<p><%= "Bio: #{user.description}" %></p>
					</div>
				</div>

			</div>
		<% end %>
	<% else %>
		<div class="welcome-tab">
			<p>Oops, seems there is noone matching "<%= @query %>"! </p>
		</div>
	<% end %>

</div>

